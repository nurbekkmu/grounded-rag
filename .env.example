# Gemini API keys (comma-separated, each from a different Google project;
# free-tier daily quotas are per key per model, so several keys help).
# Only generation and contextualization need these - retrieval, reranking,
# NLI verification and retrieval evals run fully offline.
GEMINI_API_KEYS=key1,key2
